# Sample Lecture Example

Minimal `OrganizedNotes` package demonstrating the Step 1 schema. Metadata files are complete; `image.png` crops would be added when processing real scans.

See [manifest.yaml](manifest.yaml) and [blocks/](blocks/) for structure.
